# this is a test
name: ci

on:
    push:
        branches: [main]

jobs:
    test:
        runs-on: ubuntu-latest
        
        steps:
            - name: checkout
              uses: actions/checkout@v6
            
            - name: setup nodejs
              uses: actions/setup-node@v6
              with: 
                node-version: 24
                cache: 'npm'
            
            
            